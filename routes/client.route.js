const express = require('express');
const router = express.Router();

const client_controller = require('../controllers/client.controller');

//router.get('', client_controller.all_clients);
//router.get('/:clientRef', client_controller.get_client);
//router.get('/:clientRef/orders', client_controller.get_orders);

module.exports = router;